export enum Routes {
    baseUrl = '/',
    catalog = '/catalog',
    cart = '/cart',
    authorization = '/auth',
    adminBaseUrl = '/admin'
}

export enum AdminRoutes {
    userManagement = '/admin/users'
}
