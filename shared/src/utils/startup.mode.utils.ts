export const isDevelopment = (mode?: string) => mode === 'development';
export const isProduction = (mode?: string) => mode === 'production';
export const isNotProduction = (mode?: string) => mode !== 'production';
